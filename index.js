// Define the currentUser variable
const currentUser = 'Grace Hopper';

// Define the welcomeMessage variable using concatenation
const welcomeMessage = 'Welcome to Flatbook, ' + currentUser + '!';

// Define the excitedWelcomeMessage variable using toUpperCase()
const excitedWelcomeMessage = 'WELCOME TO FLATBOOK, ' + currentUser.toUpperCase() + '!';

// Define the shortGreeting variable using string interpolation
const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`;
